<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.han.repositories.BoardMapper">
    <select id="list" resultType="com.han.models.Board">
        SELECT id, title, contents
        , created_at as createdAt
        , created_id as createdId
        FROM boards
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>
    <insert id="add">
        INSERT INTO boards (title, contents, use_yn ,created_at, created_id)
        VALUES(#{title}, #{contents}, #{use_yn}, now(), #{created_id})
    </insert>
    <update id="set">
        UPDATE boards
        SET
            title = #{title}
            ,contents = #{contents}
            ,updated_id = #{updated_id}
            ,updated_at = now()
        WHERE id = #{id}
    </update>
</mapper>
